# План разработки FastAPI приложения

## ✅ Уже выполнено

- [x] Перенесены модели (`models/base.py`, `models/product.py`)
- [x] Перенесены схемы Pydantic (`schemas/product.py`) с description и example
- [x] Перенесены CRUD операции (`core/database.py`)
- [x] Переписаны CRUD операции на асинхронные (`core/database.py`)
- [x] Создан асинхронный движок и фабрика сессий (`core/database.py`)

## 🚧 План работ

### Этап 1: Очистка проекта от Celery и Redis

Изначально в проекте были `Celery` и `Redis` для выполнения фоновых задач (как отправка уведомлений). Но сейчас наша цель — построить чистый REST API, который напрямую работает с базой данных. Фоновые задачи мы пока убираем, чтобы не усложнять.

Если это не сделать, в проекте останутся "мертвый груз": лишний код в папке `tasks/` и лишние зависимости в `requirements.txt`. Это увеличивает размер проекта, время установки и создает потенциальные риски безопасности, а также может запутать новых разработчиков.

На этом этапе мы проводим "генеральную уборку", убирая все, что не относится к нашей текущей задаче, чтобы проект стал легче, чище и понятнее.

**Файлы:**

- Удалить папку `tasks/`
- Обновить `requirements.txt`

**Действия:**

- [x] Удалить директорию `tasks/` (Celery задачи)
- [x] Убрать зависимости из `requirements.txt`: celery, redis, amqp, billiard, kombu, vine

**Коммит:** `Задача: Удалены зависимости Celery и Redis`

### Этап 2: Настройка ядра базы данных (`core/database.py`)

Сейчас в файле `core/database.py` каждая функция (например, `product_create` или `tag_delete`) сама управляет своей транзакцией с помощью декоратора `@async_with_transaction`. Это значит, что каждая такая функция открывает сессию, выполняет действие, делает `commit` и закрывает сессию.

Проблема такого подхода в том, что если несколько CRUD-операций должны быть выполнены в рамках одного API-запроса, они будут независимыми транзакциями. Если одна из них провалится, предыдущие уже будут закоммичены, что может привести к несогласованности данных.

На этом этапе мы переносим управление транзакциями с уровня каждой отдельной CRUD-функции на уровень всего HTTP-запроса в FastAPI. Мы убираем декоратор `@async_with_transaction` и `session.commit()` из CRUD-функций, а также добавляем функцию-зависимость `get_db_session()` для FastAPI. Это гарантирует, что каждый API-запрос является атомарной операцией: либо все изменения в базе данных проходят успешно, либо ни одно из них. Код становится чище, надежнее и эффективнее.

**Файлы:**

- `core/database.py`

**Действия:**

- [x] Убрать декоратор `@async_with_transaction` из всех CRUD-функций
- [x] Изменить сигнатуры CRUD-функций для приема готовой сессии
- [x] Убрать `session.commit()` из CRUD-функций
- [x] Добавить функцию-зависимость `get_db_session()` для FastAPI

#### Детально

Отлично, Володя! Вот детальный план фиксов для Этапа 2 - только список того, что нужно сделать, без кода:

## 📋 Детальный план фиксов для Этапа 2

### Файл: `core/database.py`

1. УДАЛИТЬ декоратор `@async_with_transaction`

- [x] Удалить функцию `async_with_transaction` (строки 19-46)
- [x] Удалить импорты для декоратора: `from typing import Callable, Coroutine, Any`, `from functools import wraps`

2. УБРАТЬ декоратор со всех CRUD-функций (16 функций)

**Create операции:**

- [x] `tag_create` - убрать `@async_with_transaction`
- [x] `category_create` - убрать `@async_with_transaction`  
- [x] `product_create` - убрать `@async_with_transaction`

**Delete операции:**

- [x] `category_delete` - убрать `@async_with_transaction`
- [x] `tag_delete` - убрать `@async_with_transaction`
- [x] `product_delete` - убрать `@async_with_transaction`

**Read операции:**

- [x] `category_get_by_id` - убрать `@async_with_transaction`
- [x] `tag_get_by_id` - убрать `@async_with_transaction`
- [x] `product_get_by_id` - убрать `@async_with_transaction`
- [x] `categories_get_all` - убрать `@async_with_transaction`
- [x] `tags_get_all` - убрать `@async_with_transaction`
- [x] `products_get_all` - убрать `@async_with_transaction`
- [x] `products_get_like_name` - убрать `@async_with_transaction`

**Update операции:**

- [x] `category_update` - убрать `@async_with_transaction`
- [x] `tag_update` - убрать `@async_with_transaction`
- [x] `product_update` - убрать `@async_with_transaction`

3. ИСПРАВИТЬ докстринги для всех функций

- [x] Заменить во всех докстрингах `:param session: Сессия SQLAlchemy (передаётся декоратором).` на `:param session: Асинхронная сессия SQLAlchemy.`
- [x] Убрать упоминания о декораторе из описаний

 4. ДОБАВИТЬ функцию-зависимость `get_db_session()`

- [x] Добавить функцию `async def get_db_session() -> AsyncSession`
- [x] Сигнатура: `async def get_db_session() -> AsyncGenerator[AsyncSession, None]`
- [x] Назначение: FastAPI dependency для получения сессии на каждый запрос

 5. УБРАТЬ `session.commit()` из функций (если есть)

- [x] Проверить, что ни одна функция не вызывает `await session.commit()`
- [x] В текущем коде коммиты делаются в декораторе, так что в функциях их нет

 Что НЕ нужно менять:

- ✅ Сигнатуры функций остаются прежними (принимают `session: AsyncSession`)
- ✅ Логика внутри функций остается неизменной
- ✅ Возвращаемые типы не меняются
- ✅ Импорты моделей и схем остаются

 Результат после фиксов:

- Каждая CRUD-функция будет получать готовую сессию из FastAPI
- Управление транзакциями перейдет на уровень HTTP-запроса
- Код станет чище и надежнее для сложных операций

Это все изменения для Этапа 2!

**Коммит:** `Задача: Настроено ядро базы данных`

### Этап 4: Интеграция роутов с БД (`routes/products.py`) ✅ ВЫПОЛНЕНО

Это сердце всей нашей работы. Сейчас роуты (эндпоинты) в файле `routes/products.py` берут данные из временного, захардкоженного списка `products`. На этом этапе мы наконец-то подключаем их к настоящей базе данных.

Если это не сделать, наш API останется просто "игрушкой". Он не сможет сохранять данные. Любой созданный товар будет исчезать после перезапуска сервера.

Мы заменяем временные данные на вызовы наших CRUD-функций из `core/database.py`. API становится полноценным: он сможет создавать, читать, обновлять и удалять данные, которые будут храниться в базе постоянно.

**Файлы:**

- `routes/products.py`

**Действия:**

- [x] Удалить `from data import products`
- [x] Импортировать `Depends`, `AsyncSession`, `get_db_session` и CRUD-функции
- [x] Добавить в каждый роут аргумент `session: AsyncSession = Depends(get_db_session)`
- [x] Переписать логику роутов на использование CRUD-функций
- [x] Добавить обработку ошибок `ValueError` с преобразованием в `HTTPException`
- [x] Обновить определение `APIRouter` с `prefix` и `tags`
- [x] Исправить ошибку с `item.prices.get` в `list_products`
- [x] Исправить ошибку с `product.model_dump_json` в `create_product`
- [x] Исправить ошибку с `product_update` (ожидаемые аргументы: 2)

**Коммит:** `Задача: Роуты интегрированы с базой данных`

### Этап 6: Настройка Alembic

Наш код (SQLAlchemy модели) — это "чертеж" нашей базы данных. Но что, если мы захотим изменить этот "чертеж"? Например, добавить колонку `price` в таблицу продуктов. Как обновить уже существующую базу данных в соответствии с новым чертежом?

Если это не сделать, придется заходить в базу данных вручную и писать SQL-команды (`ALTER TABLE ...`). Это рискованно (легко ошибиться), сложно отслеживать (кто, когда и что поменял?) и практически невозможно повторить на разных машинах (у себя, у коллеги, на сервере) без ошибок.

`Alembic` — это система контроля версий для базы данных, как Git для кода. Он смотрит на наши модели, сравнивает их с реальной базой и сам генерирует скрипты для обновления. Мы просто запускаем команду, и наша база данных безопасно и предсказуемо обновляется до нужной версии.

**Файлы:**

- `requirements.txt`
- Создать `alembic.ini` и папку `alembic/`

**Действия:**

- [x] Добавить `alembic` в `requirements.txt`
- [x] Выполнить `alembic init alembic` для создания структуры
- [x] Настроить `alembic.ini` и `alembic/env.py` для асинхронной БД
- [x] Создать стартовую миграцию

**Коммит:** `Задача: Настроен Alembic для миграций`

### Этап 5: Настройка логгера

Когда приложение работает на сервере, у нас нет доступа к консоли, чтобы видеть `print()`. Логгер — это профессиональный инструмент для записи всего, что происходит внутри приложения: кто какой эндпоинт вызвал, какие данные пришли, какие ошибки произошли.

Если это не сделать, то если на работающем сервере что-то сломается, мы будем как слепые котята. У нас не будет никакой информации о том, что привело к ошибке. Диагностика проблемы превратится в гадание на кофейной гуще.

Мы настраиваем систему логирования, которая будет записывать важные события в файлы или выводить в консоль в структурированном виде. Это абсолютно необходимо для поддержки и отладки любого серьезного приложения.

**Файлы:**

- `core/logging_config.py` (создать)
- `main.py`

**Действия:**

- [x] Создать файл `core/logging_config.py` с конфигурацией логгера
- [x] Добавить функцию `setup_logging()` для применения конфигурации
- [x] Вызвать `setup_logging()` в `main.py` при старте приложения
- [x] Раскомментировать и использовать логгер в `core/database.py`

**Коммит:** `Задача: Добавлена конфигурация логирования`

### Этап 3: Настройка main.py (Lifespan и CORS)

Этот этап состоит из двух важных частей:

1. **Lifespan (Жизненный цикл):** Наше приложение при старте создает `engine` — пул соединений с базой данных. Это как держать несколько готовых "труб" к базе, чтобы не тратить время на их открытие при каждом запросе. Когда мы останавливаем приложение (например, по `Ctrl+C`), эти "трубы" нужно аккуратно закрыть. Если этого не сделать, база данных может не узнать, что мы отключились, и будет продолжать держать эти соединения открытыми. Со временем они накопятся и "забьют" все доступные слоты, и база перестанет пускать к себе кого-либо еще. `lifespan` — это вежливый способ сказать базе данных "до свидания" при выключении приложения. Это правильное управление ресурсами.

2. **CORS (Cross-Origin Resource Sharing):** По умолчанию браузеры запрещают веб-страницам (наш будущий React-фронтенд на `http://localhost:5173`) делать запросы к API на другом "origin" (наш FastAPI-бэкенд на `http://localhost:8000`). "Origin" — это схема, хост и порт. Раз порты разные, браузер блокирует такие запросы из соображений безопасности. Если этого не сделать, мы напишем весь бэкенд, он будет отлично работать в Swagger UI, но как только мы запустим React-приложение и попробуем получить данные, в консоли браузера появится красная ошибка CORS, и ничего работать не будет. Мы заранее "договариваемся" с браузером, говоря нашему бэкенду: "Эй, запросам с адреса `http://localhost:5173` можно доверять".

**Файлы:**

- `main.py`

**Действия:**
РЕШИЛИ НЕ ДЕЛАТЬ ЭТОГО ДЛЯ ПРОСТОГО API. SONNET 4.5 СКАЗАЛ ЧТО ТЕКУЩАЯ РЕАЛИЗАЦИЯ КАНОНИЧНА И ОПТИМАЛЬНА. Добавили логгер в lifespan.

- [ ] Импортировать `engine` из `core.database`
- [ ] Добавить обработчик `lifespan` (через `@asynccontextmanager`), чтобы управлять `engine.dispose()` при выключении
- [x] Импортировать `CORSMiddleware` из `fastapi.middleware.cors`
- [x] Добавить `app.add_middleware(CORSMiddleware, ...)` с указанием адреса React-приложения (например, `http://localhost:5173`) в `allow_origins`

**Коммит:** `Задача: Настроен жизненный цикл (lifespan) и CORS`

### Этап 7: Тестирование

**Действия:**

- [ ] Протестировать все эндпоинты в Swagger UI
- [ ] Проверить работу с реальной базой данных
- [ ] Убедиться в корректной работе фоновых задач (Telegram уведомления)

**Коммит:** `Задача: Протестированы все эндпоинты`

---

## 📝 Примечания

- Все CRUD операции уже реализованы в `core/database.py` и готовы к использованию
- Модели и схемы полностью настроены и валидируются
- Асинхронная работа с базой данных настроена корректно
- Осталось интегрировать роуты с базой данных и настроить жизненный цикл приложения
