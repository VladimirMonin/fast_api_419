# FastAPI + Celery + Redis - Учебное приложение Python419

Простое API для управления товарами с фоновыми задачами через Celery.

## 🚀 Быстрый старт

### Предварительные требования

1. **Python 3.10+** установлен на вашей системе
2. **Redis для Windows** установлен и запущен
   - Скачать: [Microsoft Archive Redis](https://github.com/microsoftarchive/redis/releases)
   - Или использовать WSL2/Docker

### Установка зависимостей

```powershell
# Создание виртуального окружения
python -m venv venv

# Активация виртуального окружения (PowerShell)
.\venv\Scripts\Activate.ps1

# Установка зависимостей
pip install -r requirements.txt
```

### Настройка окружения

1. Скопируйте файл `.env.example` в `.env`:

   ```powershell
   Copy-Item .env.example .env
   ```

2. Отредактируйте `.env` и заполните переменные:

   ```env
   REDIS_HOST=localhost
   REDIS_PORT=6379
   TG_BOT_KEY=your_telegram_bot_key_here
   ```

### Запуск Redis (Windows)

**Вариант 1: Redis как служба Windows**

```powershell
# Запуск службы Redis
redis-server
```

**Вариант 2: Проверка состояния**

```powershell
# Проверка подключения к Redis
redis-cli ping
# Ответ должен быть: PONG
```

### Запуск приложения

Для работы приложения нужно запустить **ТРИ** процесса в отдельных терминалах:

#### Терминал 1: Redis Server

```powershell
redis-server
```

#### Терминал 2: FastAPI приложение

```powershell
# Активируйте виртуальное окружение
.\venv\Scripts\Activate.ps1

# Запустите FastAPI сервер
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

#### Терминал 3: Celery Worker (для Windows)

```powershell
# Активируйте виртуальное окружение
.\venv\Scripts\Activate.ps1

# ВАЖНО: На Windows обязательно используйте параметр --pool=solo
celery -A tasks.celery_app worker --pool=solo -l info
```

## 📝 Использование API

После запуска всех сервисов, API будет доступен по адресу: `http://localhost:8000`

### Документация API

- **Swagger UI**: <http://localhost:8000/docs>
- **ReDoc**: <http://localhost:8000/redoc>

### Примеры запросов

#### Получить список всех товаров

```bash
GET http://localhost:8000/products/
```

#### Получить товар по ID

```bash
GET http://localhost:8000/products/1
```

#### Создать новый товар (с фоновой задачей)

```bash
POST http://localhost:8000/products/
Content-Type: application/json

{
  "name": "Новый товар",
  "description": "Описание товара",
  "price": 999.99,
  "category": "Электроника",
  "image_url": "https://example.com/image.jpg"
}
```

При создании товара автоматически запускается фоновая задача для отправки уведомления в Telegram (имитация).

## 🔧 Структура проекта

```
fast_api_419/
├── main.py              # Основное приложение FastAPI
├── config.py            # Настройки приложения
├── data.py              # Данные товаров
├── requirements.txt     # Зависимости Python
├── .env                 # Переменные окружения (создать из .env.example)
├── routes/              # Маршруты API
│   └── products.py      # Маршруты для работы с товарами
├── schemas/             # Pydantic модели
│   └── product.py       # Схемы данных товаров
└── tasks/               # Фоновые задачи Celery
    ├── celery_app.py    # Настройка Celery
    └── notifications.py # Задачи отправки уведомлений
```

## ⚠️ Особенности работы на Windows

### Проблема: Celery не запускается

**Решение**: Всегда используйте параметр `--pool=solo`:

```powershell
celery -A tasks.celery_app worker --pool=solo -l info
```

### Проблема: Redis не работает

**Решение**:

1. Убедитесь, что Redis установлен для Windows
2. Проверьте, что служба Redis запущена
3. Альтернатива: используйте WSL2 или Docker

### Проблема: Задачи не выполняются

**Решение**:

1. Проверьте, что Redis доступен: `redis-cli ping`
2. Убедитесь, что Celery worker запущен с параметром `--pool=solo`
3. Проверьте логи в консоли Celery worker

## 🐛 Отладка

### Проверка подключения к Redis

```python
python -c "import redis; r = redis.Redis(host='localhost', port=6379); print('Redis OK' if r.ping() else 'Redis ERROR')"
```

### Проверка регистрации задач Celery

```powershell
celery -A tasks.celery_app inspect registered
```

### Мониторинг очереди задач

```powershell
# Просмотр активных задач
celery -A tasks.celery_app inspect active

# Просмотр статистики
celery -A tasks.celery_app inspect stats
```

## 📚 Дополнительные ресурсы

- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [Celery Documentation](https://docs.celeryq.dev/)
- [Redis Documentation](https://redis.io/documentation)
- [Pydantic Documentation](https://docs.pydantic.dev/)

## 📄 Лицензия

Учебный проект для демонстрации работы FastAPI с Celery и Redis.
