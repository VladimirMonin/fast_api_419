# 📚 Занятие №1: Jinja2 + HTMX (Часть 1)

## Коммит 1: Инициализация шаблонизатора и базовой структуры

---

## 🎯 Что мы делали

На этом этапе мы полностью меняем подход к веб-разработке. Раньше наш FastAPI был чисто JSON API — он отдавал данные, а фронтенд (например, React) делал из них красивые страницы. Теперь мы учим наш бэкенд **самостоятельно создавать готовые HTML-страницы**.

Это называется **Server-Side Rendering (SSR)** — рендеринг на стороне сервера. Браузер получает уже готовую страницу, а не пустой HTML с кучей JavaScript.

---

## 📦 Установка зависимостей

Первым делом мы добавили две новые библиотеки в проект.

### Что добавили в `requirements.txt`

```
jinja2==3.1.4
aiofiles==24.1.0
```

### Зачем нужна каждая библиотека

**Jinja2** — это шаблонизатор. Представь, что у тебя есть HTML-заготовка (шаблон), в которой есть "дырки" для данных:

```html
<h1>Привет, {{ username }}!</h1>
```

Jinja2 берёт эту заготовку, подставляет вместо `{{ username }}` реальное имя пользователя (например, "Володя"), и получается:

```html
<h1>Привет, Володя!</h1>
```

**aiofiles** — библиотека для асинхронной работы с файлами. FastAPI работает асинхронно, и когда нужно прочитать шаблон с диска, мы делаем это асинхронно, чтобы не блокировать сервер.

---

## 📁 Создание структуры папок

Мы создали две важные папки в корне проекта:

```
fast_api_419/
├── static/          # Сюда кладём CSS, JS, картинки
├── templates/       # Сюда кладём HTML-шаблоны
│   └── partials/    # Сюда кладём переиспользуемые кусочки HTML
```

### Зачем две отдельные папки?

**static/** — это папка для **статических файлов**. Когда браузер запрашивает `/static/style.css`, FastAPI просто отдаёт этот файл как есть, без обработки.

**templates/** — это папка для **шаблонов**. Файлы отсюда FastAPI **обрабатывает**: подставляет данные, выполняет циклы, условия, а потом отдаёт результат как HTML.

**partials/** — это подпапка для маленьких кусочков HTML, которые мы используем многократно (например, карточка товара).

---

## ⚙️ Настройка FastAPI

Теперь идём в файл `main.py` (он в корне проекта) и учим FastAPI работать со статикой и шаблонами.

### Файл: `main.py` (корень проекта)

#### Что добавили в импорты

```python
from pathlib import Path
from fastapi.staticfiles import StaticFiles
from fastapi.templating import Jinja2Templates
```

**Path** — это класс для удобной работы с путями к файлам. Вместо того, чтобы писать `"C:/Projects/fast_api_419/templates"`, мы пишем `BASE_DIR / "templates"`, и Python сам собирает правильный путь для любой операционной системы.

**StaticFiles** — это специальное приложение FastAPI, которое умеет отдавать файлы из папки. Когда браузер запросит `/static/style.css`, FastAPI найдёт `static/style.css` на диске и отдаст его.

**Jinja2Templates** — это класс, который умеет читать HTML-шаблоны, обрабатывать их (подставлять данные) и возвращать готовый HTML.

#### Что добавили в код

```python
# Определение базовой директории проекта
BASE_DIR = Path(__file__).resolve().parent

# Инициализация Jinja2 шаблонов
templates = Jinja2Templates(directory=str(BASE_DIR / "templates"))
```

**`Path(__file__)`** — это путь к текущему файлу (`main.py`).

**`.resolve()`** — превращает относительный путь в абсолютный (полный): `C:/PY/ПРИМЕРЫ КОДА/fast_api_419/main.py`.

**`.parent`** — берём родительскую папку: `C:/PY/ПРИМЕРЫ КОДА/fast_api_419/`.

Теперь `BASE_DIR` — это абсолютный путь к корню нашего проекта. Мы можем от него отталкиваться: `BASE_DIR / "templates"` → `C:/PY/ПРИМЕРЫ КОДА/fast_api_419/templates`.

**`templates = Jinja2Templates(...)`** — создаём объект, который умеет работать с шаблонами из папки `templates/`.

#### Монтирование статики

После всех роутеров (в самом конце `main.py`, перед логами) добавили:

```python
# Монтирование статических файлов (CSS, JS, изображения)
app.mount("/static", StaticFiles(directory=str(BASE_DIR / "static")), name="static")
```

**`app.mount("/static", ...)`** — это как сказать FastAPI: "Если пользователь запрашивает что-то начинающееся с `/static/`, ищи это в папке `static/` на диске".

Например:

- Запрос `/static/css/style.css` → FastAPI ищет `static/css/style.css`
- Запрос `/static/images/logo.png` → FastAPI ищет `static/images/logo.png`

**`name="static"`** — это имя для обратной генерации URL. Можно будет в коде писать `url_for("static", path="/css/style.css")`, и FastAPI сам соберёт правильный URL.

---

## 🎨 Создание базового шаблона

Теперь создаём первый и самый важный шаблон — `base.html`. Это **родительский шаблон**, от которого будут наследоваться все остальные страницы.

### Файл: `templates/base.html`

Вот полная структура этого файла:

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Магазин Python419{% endblock %}</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" 
          rel="stylesheet">
    
    <!-- HTMX -->
    <script src="https://unpkg.com/htmx.org@1.9.10"></script>
    
    {% block extra_head %}{% endblock %}
</head>
<body>
    <!-- Навигационная панель -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <strong>🛒 Python419 Shop</strong>
            </a>
            <!-- Остальная навигация -->
        </div>
    </nav>

    <!-- Основной контент -->
    <main class="container my-4">
        {% block content %}
        <!-- Контент страницы будет вставлен здесь -->
        {% endblock %}
    </main>

    <!-- Футер -->
    <footer class="bg-light text-center text-muted py-4 mt-5">
        <div class="container">
            <p class="mb-0">© 2025 Python419 Shop</p>
        </div>
    </footer>

    <!-- Bootstrap 5 JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    {% block extra_scripts %}{% endblock %}
</body>
</html>
```

### Разбор по частям

#### 1. Подключение Bootstrap и HTMX

```html
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
<script src="https://unpkg.com/htmx.org@1.9.10"></script>
```

**Bootstrap** — это CSS-фреймворк. Он даёт нам готовые красивые классы для кнопок, карточек, сеток и т.д. Мы подключаем его через **CDN** (Content Delivery Network) — это быстрый сервер с библиотеками.

**HTMX** — это маленькая JS-библиотека, которая позволяет делать динамические запросы на сервер **без написания JavaScript-кода**. Просто добавляешь специальные атрибуты в HTML, и HTMX делает всю работу.

#### 2. Блоки Jinja2

```html
{% block title %}Магазин Python419{% endblock %}
```

**Блок** — это "дырка" в шаблоне, которую можно переопределить в дочерних шаблонах.

Например, если страница наследуется от `base.html` и пишет:

```html
{% block title %}Каталог товаров{% endblock %}
```

То в итоговом HTML будет:

```html
<title>Каталог товаров</title>
```

У нас есть четыре блока:

- **`title`** — заголовок страницы в браузере
- **`extra_head`** — для дополнительных CSS или мета-тегов
- **`content`** — основной контент страницы
- **`extra_scripts`** — для дополнительных JS-скриптов

#### 3. Структура страницы

```
┌─────────────────────────────┐
│   <nav> Навигация           │  ← Шапка сайта
├─────────────────────────────┤
│                             │
│   <main> {% block content %}│  ← Основной контент
│                             │
├─────────────────────────────┤
│   <footer> Футер            │  ← Подвал сайта
└─────────────────────────────┘
```

---

## 🛤️ Создание роутера для страниц

Теперь создаём отдельный роутер, который будет отдавать HTML-страницы (а не JSON).

### Файл: `routes/pages.py`

```python
from fastapi import APIRouter, Request
from fastapi.templating import Jinja2Templates
from pathlib import Path

# Определение базовой директории и шаблонов
BASE_DIR = Path(__file__).resolve().parent.parent
templates = Jinja2Templates(directory=str(BASE_DIR / "templates"))

router = APIRouter()

@router.get("/", include_in_schema=False)
async def index(request: Request):
    """Главная страница - каталог товаров"""
    return templates.TemplateResponse(
        "index.html",
        {
            "request": request,
            "message": "Привет из Jinja2! 👋"
        }
    )
```

### Разбор

#### 1. Путь к шаблонам

```python
BASE_DIR = Path(__file__).resolve().parent.parent
```

**`Path(__file__)`** — путь к текущему файлу (`routes/pages.py`).

**`.parent`** — родительская папка (`routes/`).

**`.parent.parent`** — ещё раз родительская папка → корень проекта.

#### 2. Функция-обработчик

```python
@router.get("/", include_in_schema=False)
async def index(request: Request):
```

**`include_in_schema=False`** — это говорит FastAPI: "Не добавляй этот эндпоинт в автоматическую документацию (Swagger)". Зачем? Потому что документация ожидает JSON, а мы возвращаем HTML.

**`request: Request`** — это объект запроса. Jinja2 требует его для правильной работы (нужен для генерации URL и других вещей).

#### 3. Возврат шаблона

```python
return templates.TemplateResponse(
    "index.html",
    {
        "request": request,
        "message": "Привет из Jinja2! 👋"
    }
)
```

**`templates.TemplateResponse(...)`** — это функция, которая:

1. Находит файл `templates/index.html`
2. Подставляет в него данные из словаря (`message`, `request`)
3. Возвращает готовый HTML

Словарь данных называется **контекст**. Все переменные из контекста доступны в шаблоне через `{{ имя_переменной }}`.

---

## 📄 Создание первого шаблона страницы

### Файл: `templates/index.html`

```html
{% extends "base.html" %}

{% block title %}Главная - Python419 Shop{% endblock %}

{% block content %}
<div class="row">
    <div class="col-12">
        <h1 class="mb-4">🎉 {{ message }}</h1>
        <div class="alert alert-success" role="alert">
            <h4 class="alert-heading">Отличная работа!</h4>
            <p>Шаблонизатор Jinja2 успешно подключен и работает.</p>
        </div>
    </div>
</div>
{% endblock %}
```

### Как работает наследование

```
base.html (родитель)          index.html (наследник)
┌──────────────────┐          ┌──────────────────┐
│ <head>           │          │ extends base.html│
│   {% block title│  ←──────────  block title   │
│      Магазин    │          │    Главная       │
│   {% endblock   │          │  endblock        │
│ </head>          │          └──────────────────┘
│                  │
│ <main>           │          ┌──────────────────┐
│   {% block      │  ←──────────  block content  │
│      content    │          │    <h1>...       │
│   {% endblock   │          │  endblock        │
│ </main>          │          └──────────────────┘
└──────────────────┘
```

**`{% extends "base.html" %}`** — это как сказать: "Я беру всю структуру из `base.html` и только заменяю нужные блоки".

**`{{ message }}`** — это переменная из контекста. Мы передали `"message": "Привет из Jinja2! 👋"`, поэтому в HTML будет `<h1>🎉 Привет из Jinja2! 👋</h1>`.

---

## 🔌 Подключение роутера в main.py

В конце файла `main.py` добавили:

```python
from routes import categories, products, tags, cart, orders, pages

# ... остальные роутеры ...

# HTML страницы (должны быть подключены ПОСЛЕДНИМИ)
app.include_router(pages.router, tags=["Pages"])
```

### Почему последними?

Потому что роут `GET /` в `pages.py` может перехватить запросы, предназначенные для API. Роутеры проверяются **сверху вниз**, поэтому API-роутеры должны быть выше.

---

## 🎯 Итоговая схема работы

```
Браузер                 FastAPI                  Файловая система
  │                       │                            │
  │  GET /                │                            │
  ├──────────────────────>│                            │
  │                       │                            │
  │                       │  pages.index()             │
  │                       │  ↓                         │
  │                       │  templates.TemplateResponse│
  │                       │  ↓                         │
  │                       │  Ищет templates/index.html │
  │                       ├───────────────────────────>│
  │                       │                            │ templates/index.html
  │                       │<───────────────────────────┤
  │                       │                            │
  │                       │  Видит extends base.html   │
  │                       ├───────────────────────────>│
  │                       │                            │ templates/base.html
  │                       │<───────────────────────────┤
  │                       │                            │
  │                       │  Собирает HTML:            │
  │                       │  base + блоки из index     │
  │                       │  Подставляет {{ message }} │
  │                       │  ↓                         │
  │  HTML страница        │                            │
  │<──────────────────────┤                            │
  │                       │                            │
```

---

## ✅ Что мы получили

После этого коммита:

- ✅ FastAPI умеет отдавать статические файлы из `/static/`
- ✅ FastAPI умеет рендерить Jinja2 шаблоны
- ✅ Создан базовый шаблон `base.html` с Bootstrap и HTMX
- ✅ Создан первый роутер `pages.py` для HTML-страниц
- ✅ Создана тестовая страница `index.html` с наследованием

Теперь при открытии `http://127.0.0.1:8000/` мы видим красивую HTML-страницу с Bootstrap-стилями и приветственным сообщением! 🎉
