# 📚 Занятие №2: Cookie-Based Authentication (Часть 7)

## Коммит: HTMX редактирование товаров прямо в карточке

---

## 🎯 Что мы делали

До этого момента редактирование товара требовало перехода на отдельную страницу: нажать кнопку "Редактировать", подождать загрузки `/admin/products/{id}/edit`, изменить данные, нажать "Сохранить", дождаться редиректа на главную. Это классический веб-флоу, но он громоздкий для быстрых правок.

Современные пользователи ожидают более плавного опыта. Представьте админа, который видит опечатку в названии товара. Вместо перехода на другую страницу он мог бы нажать иконку редактирования прямо на карточке товара, изменить поле в inline-форме, и сразу увидеть результат без перезагрузки.

Это называется **inline editing** — редактирование "на месте". HTMX делает это тривиально простым через механизм замены элементов. Карточка товара превращается в форму. После сохранения форма превращается обратно в карточку. Никаких редиректов, никаких полных перезагрузок страницы.

---

## ✏️ Добавление кнопки редактирования в карточку

Первый шаг — модифицировать `templates/partials/product_card.html`, добавив кнопку, которая видна только админам:

```html
<div class="card">
    <div class="card-body">
        <h5>{{ product.name }}</h5>
        <p>{{ product.description }}</p>
        
        {% if user and user.is_superuser %}
            <button class="btn btn-sm btn-outline-primary"
                    hx-get="/admin/products/{{ product.id }}/edit"
                    hx-target="closest .card"
                    hx-swap="outerHTML">
                ✏️ Редактировать
            </button>
        {% endif %}
    </div>
</div>
```

Условие `{% if user and user.is_superuser %}` гарантирует, что кнопка видна только залогиненным админам. Обычные пользователи и гости не увидят её вообще.

Атрибут `hx-get="/admin/products/{{ product.id }}/edit"` говорит HTMX: при клике отправить GET-запрос на этот URL. Например, для товара с ID=5 это будет `/admin/products/5/edit`.

Но подождите, этот роут у нас уже есть! Мы создали его в прошлой части для отображения полной страницы редактирования. Теперь мы будем использовать его **двумя способами**.

Если пользователь переходит на `/admin/products/5/edit` через адресную строку или обычную ссылку, он получает **полную страницу** с навбаром и формой. Если HTMX делает запрос на тот же URL, он получает **только форму** без обёртки.

Как сервер узнаёт разницу? Через HTTP-заголовок `HX-Request`. HTMX автоматически добавляет этот заголовок ко всем своим запросам. На бэкенде мы проверяем его и возвращаем либо полный шаблон, либо partial.

Атрибут `hx-target="closest .card"` определяет, что заменить. `closest .card` означает: найди ближайший родительский элемент с классом `card`. В нашем случае это сама карточка товара.

Атрибут `hx-swap="outerHTML"` говорит: заменить элемент целиком, включая сам тег `<div class="card">`. Результат GET-запроса (форма) займёт место карточки.

---

## 📝 Создание компактной формы редактирования

Теперь нужен новый шаблон для inline-формы: `templates/partials/product_edit_form.html`. Этот шаблон **не наследуется** от `base.html` — это partial, который будет вставлен в существующую страницу.

Форма практически идентична `admin_product.html`, но с несколькими отличиями:

```html
<div class="card">
    <div class="card-body">
        <form hx-post="/admin/products/{{ product.id }}/edit"
              hx-target="closest .card"
              hx-swap="outerHTML">
            
            <input type="text" name="name" value="{{ product.name }}" required>
            <input type="number" name="price_shmeckles" value="{{ product.price_shmeckles }}" step="0.01" required>
            <!-- ... остальные поля ... -->
            
            <button type="submit" class="btn btn-primary">Сохранить</button>
            <button type="button" class="btn btn-secondary"
                    hx-get="/products/{{ product.id }}/card"
                    hx-target="closest .card"
                    hx-swap="outerHTML">
                Отмена
            </button>
        </form>
    </div>
</div>
```

Форма обёрнута в `<div class="card">`, чтобы структура DOM оставалась консистентной. Когда мы заменяем карточку формой или форму карточкой, всегда меняется элемент с классом `card`.

Атрибут `hx-post="/admin/products/{{ product.id }}/edit"` отправляет данные на тот же роут, что мы использовали раньше. Разница в том, что теперь сервер должен вернуть не редирект, а обновлённую карточку товара.

Кнопка "Отмена" имеет `type="button"` (не submit!), чтобы не отправлять форму. Атрибут `hx-get="/products/{{ product.id }}/card"` запрашивает карточку товара. Это новый роут, который возвращает только HTML карточки без обёртки страницы.

`hx-target="closest .card"` и `hx-swap="outerHTML"` делают то же самое: заменяют форму карточкой. Визуально это выглядит как "закрытие" формы без сохранения.

---

## 🔄 Модификация роута редактирования

Теперь роут `/admin/products/{id}/edit` должен работать двумя способами. Если запрос приходит от HTMX, возвращаем partial. Если обычный — полную страницу.

Проверяем заголовок `HX-Request`:

```python
@router.get("/products/{product_id}/edit")
async def edit_product_page(
    product_id: int,
    request: Request,
    user: User = Depends(require_superuser),
    session: AsyncSession = Depends(get_session)
):
    product = await get_product_by_id(session, product_id)
    if not product:
        raise HTTPException(404, "Товар не найден")
    
    await session.refresh(product, ["tags"])
    
    # Если запрос от HTMX, возвращаем только форму
    if request.headers.get("HX-Request"):
        categories = await get_all_categories(session)
        tags = await get_all_tags(session)
        return templates.TemplateResponse("partials/product_edit_form.html", {
            "request": request,
            "product": product,
            "categories": categories,
            "tags": tags
        })
    
    # Иначе возвращаем полную страницу
    categories = await get_all_categories(session)
    tags = await get_all_tags(session)
    return templates.TemplateResponse("admin_product.html", {
        "request": request,
        "user": user,
        "product": product,
        "categories": categories,
        "tags": tags
    })
```

Выражение `request.headers.get("HX-Request")` возвращает значение заголовка или `None`, если заголовка нет. HTMX устанавливает этот заголовок в `"true"`, что является "truthy" значением в Python.

Теперь тот же URL даёт разные результаты в зависимости от того, кто его запрашивает. Это называется **content negotiation** — выбор формата ответа на основе контекста запроса.

---

## 🎯 Модификация POST-роута для inline-редактирования

Аналогично, POST-роут должен возвращать обновлённую карточку вместо редиректа, если запрос от HTMX:

```python
@router.post("/products/{product_id}/edit")
async def edit_product(
    product_id: int,
    request: Request,
    name: str = Form(...),
    # ... остальные параметры ...
    user: User = Depends(require_superuser),
    session: AsyncSession = Depends(get_session)
):
    # ... обновление товара в БД ...
    
    await session.commit()
    
    # Если запрос от HTMX, возвращаем обновлённую карточку
    if request.headers.get("HX-Request"):
        await session.refresh(product_orm, ["tags", "category"])
        return templates.TemplateResponse("partials/product_card.html", {
            "request": request,
            "product": product_orm,
            "user": user
        })
    
    # Иначе редирект на главную
    return RedirectResponse("/", status_code=303)
```

После сохранения изменений делаем `session.refresh(product_orm, ["tags", "category"])`, чтобы загрузить связанные данные. Затем рендерим `product_card.html` с обновлённым товаром.

HTMX получает HTML карточки и заменяет форму. Пользователь видит: форма исчезла, на её месте карточка с новыми данными. Всё произошло без перезагрузки страницы.

Если кто-то отправит форму обычным способом (например, через curl без заголовка `HX-Request`), сработает старое поведение — редирект на главную.

---

## 🆕 Роут для получения карточки товара

Для кнопки "Отмена" нужен роут, который возвращает только карточку:

```python
@router.get("/products/{product_id}/card")
async def get_product_card(
    product_id: int,
    request: Request,
    user: User | None = Depends(get_current_user_from_cookie),
    session: AsyncSession = Depends(get_session)
):
    product = await get_product_by_id(session, product_id)
    if not product:
        raise HTTPException(404, "Товар не найден")
    
    await session.refresh(product, ["tags", "category"])
    
    return templates.TemplateResponse("partials/product_card.html", {
        "request": request,
        "product": product,
        "user": user
    })
```

Этот роут **не требует** прав админа, потому что карточки товаров видны всем. Мы используем `user: User | None`, а не `require_superuser`.

Загружаем товар, подгружаем связанные данные, рендерим partial. HTMX заменяет форму на карточку, визуально "отменяя" редактирование.

---

## 🎭 Как это выглядит для пользователя

Давайте проследим весь флоу inline-редактирования с точки зрения админа.

Админ открывает главную страницу. На каждой карточке товара он видит иконку карандаша ✏️. Обычные пользователи её не видят — условие `{% if user.is_superuser %}` скрыло кнопку.

Админ кликает на карандаш. HTMX отправляет GET на `/admin/products/5/edit`. Сервер проверяет заголовок `HX-Request`, видит его, возвращает partial `product_edit_form.html`.

HTMX получает HTML формы и выполняет `hx-swap="outerHTML"` на `closest .card`. Карточка товара **исчезает**, на её месте появляется **форма**. Поля предзаполнены текущими значениями.

Админ меняет название с "Portal Gun" на "Portal Gun v2", меняет цену, нажимает "Сохранить". HTMX отправляет POST на `/admin/products/5/edit` с FormData.

Сервер обновляет товар в БД, проверяет заголовок `HX-Request`, рендерит обновлённую карточку `product_card.html`, возвращает её.

HTMX получает HTML карточки, заменяет форму. Админ видит: форма **исчезла**, на её месте **карточка** с новыми данными. Название теперь "Portal Gun v2", цена обновилась.

Если админ передумал и нажал "Отмена", HTMX запрашивает `/products/5/card`, получает исходную карточку (без изменений), заменяет форму. Визуально то же самое, но данные не сохранились.

Всё это происходит **без единой перезагрузки** страницы. Скролл позиция не сбрасывается. Другие карточки на странице не мелькают. Изменяется только один элемент — та карточка, которую редактируем.

---

## 🧩 Паттерн: замена элементов через outerHTML

HTMX использует мощный паттерн: вместо манипуляций с DOM через JavaScript, сервер отправляет готовый HTML, который заменяет часть страницы.

В React или Vue мы бы писали:

```javascript
function handleEdit() {
  setEditMode(true)
}
function handleSave() {
  await api.updateProduct(data)
  setEditMode(false)
  refetchProduct()
}
```

Два состояния (карточка/форма), логика переключения на клиенте, управление данными через state, ручная синхронизация с сервером.

В HTMX мы просто **заменяем HTML**:

```
Карточка → (click) → GET /edit → HTML формы → swap
Форма → (submit) → POST /edit → HTML карточки → swap
Форма → (cancel) → GET /card → HTML карточки → swap
```

Нет состояния на клиенте. Сервер всегда знает правду. Клиент просто отображает то, что сервер прислал.

Это упрощает ментальную модель: сервер контролирует UI. Также это делает приложение более устойчивым к багам — невозможна рассинхронизация между клиентским state и серверными данными.

---

## 💡 Резюме

Мы реализовали inline-редактирование товаров через HTMX. Добавили кнопку редактирования в карточку, создали компактную форму в partial шаблоне, модифицировали роуты для поддержки двух режимов работы (полная страница или partial).

Ключевые концепции: проверка заголовка `HX-Request` для выбора шаблона, `hx-target="closest .card"` для таргетирования родительского элемента, `hx-swap="outerHTML"` для полной замены элемента, паттерн карточка↔форма через замену HTML.

Этот подход масштабируется: любой элемент на странице может превращаться в форму и обратно. Комментарии, профили пользователей, настройки — всё можно редактировать inline с помощью того же паттерна.

В следующей финальной части мы разберём все баги, которые возникли в процессе разработки, и как мы их исправили.
