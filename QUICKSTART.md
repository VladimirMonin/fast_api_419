# 🚀 Быстрая шпаргалка по запуску проекта

## Подготовка (один раз)

```powershell
# 1. Создать виртуальное окружение
python -m venv venv

# 2. Активировать виртуальное окружение
.\venv\Scripts\Activate.ps1

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Создать файл .env из примера
Copy-Item .env.example .env

# 5. Отредактировать .env (указать настройки Redis и Telegram bot key)
notepad .env
```

---

## Запуск проекта (каждый раз)

### ⚡ Нужно 3 терминала одновременно

#### Терминал 1️⃣: Redis

```powershell
redis-server
```

#### Терминал 2️⃣: FastAPI

```powershell
.\venv\Scripts\Activate.ps1
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

#### Терминал 3️⃣: Celery Worker ⚠️ (ВАЖНО для Windows!)

```powershell
.\venv\Scripts\Activate.ps1
celery -A tasks.celery_app worker --pool=solo -l info
```

> **КРИТИЧНО**: На Windows **обязательно** используйте параметр `--pool=solo`!

---

## 🔍 Проверка работы

### Проверить Redis

```powershell
redis-cli ping
# Ответ: PONG
```

### Проверить FastAPI

Откройте в браузере: <http://localhost:8000/docs>

### Проверить Celery

```powershell
celery -A tasks.celery_app inspect registered
```

---

## 📋 Частые команды

### Мониторинг Celery

```powershell
# Активные задачи
celery -A tasks.celery_app inspect active

# Статистика
celery -A tasks.celery_app inspect stats

# Очистить очередь задач
celery -A tasks.celery_app purge
```

### Проверка подключения к Redis из Python

```powershell
python -c "import redis; r = redis.Redis(host='localhost', port=6379); print('Redis OK' if r.ping() else 'Redis ERROR')"
```

---

## ❌ Решение типичных проблем

### Проблема: "Received unregistered task"

**Решение**:

1. Перезапустите Celery worker
2. Убедитесь, что используете `--pool=solo`
3. Проверьте, что файл `tasks/notifications.py` не содержит ошибок

### Проблема: "OSError: [WinError 6]"

**Решение**: Используйте `--pool=solo` при запуске Celery

### Проблема: Redis не отвечает

**Решение**:

1. Убедитесь, что Redis запущен: `redis-cli ping`
2. Проверьте порт в `.env` (по умолчанию 6379)
3. Перезапустите службу Redis

---

## 🌐 Полезные ссылки

- Swagger UI: <http://localhost:8000/docs>
- ReDoc: <http://localhost:8000/redoc>
- API Root: <http://localhost:8000/>

---

## 💡 Советы

1. **Всегда запускайте в таком порядке**: Redis → FastAPI → Celery
2. **При изменении кода**: FastAPI перезапустится автоматически (--reload), но Celery нужно перезапускать вручную
3. **Логи Celery**: Смотрите в терминале 3️⃣, там будет информация о выполнении задач
4. **Остановка всех процессов**: Нажмите `Ctrl+C` в каждом терминале
